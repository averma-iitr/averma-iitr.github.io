{
  "name": "averma.github.io",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "deploy": "npm run build && cp -R .next/static docs/ && cp -R out/* docs/ && touch docs/.nojekyll"
  },
  "dependencies": {
    "@reduxjs/toolkit": "^2.2.4",
    "next": "14.2.3",
    "react": "^18",
    "react-dom": "^18",
    "react-redux": "^9.1.2"
  },
  "devDependencies": {
    "eslint": "^8",
    "eslint-config-next": "14.2.3"
  }
}
